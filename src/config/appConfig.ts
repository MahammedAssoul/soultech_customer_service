/**
 * Central application configuration.
 *
 * Set `isMockService` to `true` to run the entire application on local mock
 * data (no Supabase, no network, works offline).
 * Set it to `false` to use the real Supabase services.
 */
export const isMockService = true